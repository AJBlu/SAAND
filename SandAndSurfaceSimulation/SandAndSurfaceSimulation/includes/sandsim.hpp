#pragma once
#include <SDL3/SDL.h>

#include <random>
#include <fstream>
/*
class Sim
{
	public:
		enum class Material : Uint8 {
			EMPTY = 0,
			SOLID,
			SAND,
			WATER
		};

		enum class Direction : Uint8 {
			NW = 0,
			N,
			NE,
			E,
			SE,
			S,
			SW,
			W,
			NO_DIRECTION = 255
		};

		Sim(int _width, int _height, SDL_PixelFormat _pixelFormat);
		~Sim();
		void update();
		void reset();
		//void pixelFormatDetail(SDL_PixelFormat _pixelFormat) { pixelFormatDetail = SDL_GetPixelFormatDetails(_pixelFormat); }

	private:
		int _width, _height;
		int _size;
		SDL_PixelFormatDetails* pixelFormatDetail;
};
*/